logging {
    level  = "info"
    format = "logfmt"
    write_to = [loki.write.default.receiver]
}

loki.write "default" {
    endpoint {
        url = "https://" + sys.env("ALLOY_LOKI_USERNAME") + ":" + sys.env("ALLOY_LOKI_PASSWORD") + "@logs.famillejouve.ch/loki/api/v1/push"
    }
}
